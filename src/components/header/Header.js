import React, { Component } from 'react';
import { PageHeader } from 'react-bootstrap';

class Header extends Component {
  render() {
    return (
      <PageHeader>
        CareerAtlas <small>Find your next career move</small>
      </PageHeader>
    )
  }
}

export default Header;
